---
import type { HTMLAttributes } from 'astro/types';

export interface Props extends HTMLAttributes<'a'> {
    class?: string
}
const { class: className, ...rest } = Astro.props
---
<a class:list={["flex items-center gap-2 link", className]} {...rest}>
    <slot />
</a>